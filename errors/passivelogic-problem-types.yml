generated: '2026-08-04'
method: derived
source: openapi/passivelogic-rest-api-openapi.yml
format: custom
rfc9457: false
note: >-
  The PassiveLogic OpenAPI declares exactly one response code — 200 — on all 134 documented operations. There is no
  4xx or 5xx response, no schema for a failure body, and no error reference in the Quantum documentation. The
  envelope below was observed on live anonymous probes, not read out of the spec, and is recorded as such. Anything
  beyond these observed statuses is unknown, and is deliberately not listed.
envelope:
  content_type: application/json; charset=utf-8
  fields:
  - name: error
    type: boolean
    description: Always true on a failure response.
  - name: reason
    type: string
    description: Human-readable failure reason. No machine-readable code accompanies it.
problems:
- status: 401
  title: Unauthorized
  observed_reason: Could not find hive or user.
  observed_on: POST https://passivelogic.com/api/graphql
  remediation: >-
    Supply a PL-API-KEY header or an X-PL-AUTH JWT. Obtain a JWT with a basic-auth GET to /api/auth/login, or an API
    key with GET /api/auth/api-key/generate as an authenticated user.
  method: probed
- status: 401
  title: Unauthorized
  observed_reason: Unauthorized
  observed_on: POST https://quantumalliance.org/api/graphql
  method: probed
- status: 404
  title: Not Found
  observed_reason: Not Found
  observed_on: GET https://passivelogic.com/api/auth/whoami (unauthenticated)
  remediation: >-
    /api/auth/whoami answers 404 rather than 401 for an unauthenticated caller — the identity route is hidden, not
    challenged. Authenticate first.
  method: probed
graphql_errors:
  schema: GraphQLError
  source: openapi/passivelogic-rest-api-openapi.yml#/components/schemas/GraphQLError
  fields:
  - name: message
    type: string
  - name: locations
    type: array
    items: SourceLocation
  - name: path
    type: array
  note: >-
    GraphQL failures follow the GraphQL spec errors[] array (message + locations + path) rather than the REST
    error/reason envelope. The GraphQLResult schema carries data and errors side by side.
gaps:
  - No 4xx/5xx responses are declared in the OpenAPI, so generated clients cannot type failure handling.
  - No stable machine-readable error code accompanies `reason`, so callers must string-match.
  - No RFC 9457 application/problem+json media type is used.
  - No published error reference page.
x-evidence:
  fetched: '2026-08-04'
  probes:
  - url: https://passivelogic.com/api/graphql
    http_status: 401
  - url: https://quantumalliance.org/api/graphql
    http_status: 401
  - url: https://passivelogic.com/api/auth/whoami
    http_status: 404
